# Anthropic Claude Target Configuration

- **Target Key**: `claude`
- **Primary Directive**: `CLAUDE.md`
- **Skills Path**: `.claude/skills/`
- **Synchronization**: Maintained via `sde skill sync` or `python tools/skill_builder.py sync`.
- **Command Style**: Terminal commands using `sde` or `python tools/...`.
